{
  "name": "websocket-server",
  "version": "1.0.0",
  "description": "Servidor WebSocket com autenticação por token",
  "main": "servidor.js",
  "scripts": {
    "start": "node servidor.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "ws": "^8.13.0"
  }
}
